{ nix-colors, ... }:

{
  # colorScheme = nix-colors.colorSchemes.tokyo-night-dark;
  # colorScheme = nix-colors.colorSchemes.tokyo-city-dark;
  # colorScheme = nix-colors.colorSchemes.gruvbox-dark-soft;
  # colorScheme = nix-colors.colorSchemes.gruvbox-dark-medium;
  # colorScheme = nix-colors.colorSchemes.gruvbox-dark-hard;
  # colorScheme = nix-colors.colorSchemes.gruvbox-dark-pale;
  colorScheme = nix-colors.colorSchemes.catppuccin-mocha;
  # colorScheme = nix-colors.colorSchemes.nord;
  # colorScheme = nix-colors.colorSchemes.ayu-dark;
  # colorScheme = nix-colors.colorSchemes.black-metal-dark-funeral;
  # colorScheme = nix-colors.colorSchemes.black-metal-immortal;
  # colorScheme = nix-colors.colorSchemes.google-dark;
  # colorScheme = nix-colors.colorSchemes.material-darker;
  # colorScheme = nix-colors.colorSchemes.onedark;
  # colorScheme = nix-colors.colorSchemes.solarized-dark;
  # colorScheme = nix-colors.colorSchemes.windows-95;
}
